import SwiftUI

struct NETStomachScreen: View {
    @Environment(\.dismiss) private var dismiss
    
    @State private var showGuidelines = false

    var body: some View {
        VStack(spacing: 0) {
            // Top Bar
            HStack {
                Button(action: { dismiss() }) {
                    Image(systemName: "arrow.left")
                        .resizable()
                        .frame(width: 26, height: 26)
                        .foregroundColor(.black)
                }
                Spacer()
                Text("GIT")
                    .font(.system(size: 26, weight: .bold))
                Spacer()
                Button(action: {
                    showGuidelines = true
                }) {
                    Image(systemName: "ellipsis")
                        .font(.system(size: 26, weight: .bold))
                        .foregroundColor(.black)
                }
            }
            .padding([.horizontal, .top], 18)
            .padding(.bottom, 4)
            
            // Subtitle Section
            Text("NET(Neuro Endocrine\nTumor )")
                .font(.system(size: 18))
                .multilineTextAlignment(.center)
                .padding(.top, 10)

            Text("Stomach")
                .font(.system(size: 17))
                .multilineTextAlignment(.center)
                .padding(.top, 6)

            Spacer().frame(height: 38)

            // Info Box
            HStack {
                Spacer()
                VStack(alignment: .center, spacing: 14) {
                    Text("<3% - Well differentiated\nneuroendocrine tumour -\ngrade 1")
                        .font(.system(size: 17))
                        .foregroundColor(.white)
                        .multilineTextAlignment(.center)
                    Text("3–20 - Well differentiated\nneuroendocrine tumour-grade\n2")
                        .font(.system(size: 17))
                        .foregroundColor(.white)
                        .multilineTextAlignment(.center)
                    Text(">20% - Well differentiated\nneuroendocrine tumour -\ngrade 3")
                        .font(.system(size: 17))
                        .foregroundColor(.white)
                        .multilineTextAlignment(.center)
                }
                .padding(.vertical, 28)
                .padding(.horizontal, 10)
                .frame(maxWidth: .infinity)
                .background(Color(red: 0.51, green: 0.84, blue: 1.0))
                .cornerRadius(12)
                Spacer()
            }
            .padding(.horizontal, 24)

            Spacer()
            
            // NavigationLink for Ellipsis button action
            NavigationLink(destination: NETguidelines(), isActive: $showGuidelines) {
                EmptyView()
            }
            .hidden()
        }
        .background(Color.white.ignoresSafeArea())
        .navigationBarBackButtonHidden(true)
    }
}

struct NETStomachScreen_Previews: PreviewProvider {
    static var previews: some View {
        NavigationStack {
            NETStomachScreen()
        }
    }
}
