import SwiftUI

struct GITScreen: View {
    @Environment(\.dismiss) private var dismiss

    var body: some View {
        NavigationStack {
            VStack(spacing: 0) {
                // Top Bar
                HStack {
                    Button(action: { dismiss() }) {
                        Image(systemName: "arrow.left")
                            .resizable()
                            .frame(width: 26, height: 26)
                            .foregroundColor(.black)
                    }
                    Spacer()
                    Text("GIT")
                        .font(.system(size: 30, weight: .medium))
                    Spacer()
                    Spacer().frame(width: 26)
                }
                .padding(.horizontal, 18)
                .padding(.top, 12)

                Spacer().frame(height: 38)

                VStack(spacing: 38) {
                    NavigationLink(destination: Adenocarcinoma()) {
                        Text("Adenocarcinoma")
                            .font(.system(size: 22, weight: .medium))
                            .foregroundColor(.white)
                            .padding(.vertical, 16)
                            .frame(maxWidth: .infinity)
                            .background(Color(red: 0.54, green: 0.87, blue: 1.0))
                            .cornerRadius(8)
                    }

                    NavigationLink(destination: GIT_NETScreen()) {
                        Text("NET(Neuroendocrine tumour)")
                            .font(.system(size: 20, weight: .medium))
                            .foregroundColor(.white)
                            .multilineTextAlignment(.center)
                            .padding(.vertical, 16)
                            .frame(maxWidth: .infinity)
                            .background(Color(red: 0.54, green: 0.87, blue: 1.0))
                            .cornerRadius(8)
                    }

                    NavigationLink(destination: GISTSelectorScreen()) {
                        Text("GIST(Gastro intestinal\nStromal Tumour)")
                            .font(.system(size: 20, weight: .medium))
                            .foregroundColor(.white)
                            .multilineTextAlignment(.center)
                            .padding(.vertical, 16)
                            .frame(maxWidth: .infinity)
                            .background(Color(red: 0.54, green: 0.87, blue: 1.0))
                            .cornerRadius(8)
                    }
                }
                .frame(maxWidth: 350)
                .padding(.top, 26)

                Spacer()
            }
            .background(Color.white.ignoresSafeArea())
            .navigationBarBackButtonHidden(true)
            .navigationBarHidden(true)
        }
    }
}

struct GITScreen_Previews: PreviewProvider {
    static var previews: some View {
        GITScreen()
    }
}
