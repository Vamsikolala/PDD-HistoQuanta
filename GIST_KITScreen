import SwiftUI

struct GIST_KITScreen: View {
    @Environment(\.dismiss) private var dismiss

    var body: some View {
        VStack(spacing: 0) {
            // Top Bar
            HStack {
                Button(action: { dismiss() }) {
                    Image(systemName: "arrow.left")
                        .resizable()
                        .frame(width: 26, height: 26)
                        .foregroundColor(.black)
                }
                Spacer()
            }
            .padding(.leading, 18)
            .padding(.top, 18)
            .padding(.bottom, 4)

            // Title
            Text("GIT")
                .font(.system(size: 26, weight: .bold))
                .padding(.top, 8)

            // Subtitle
            Text("GIST (Gastro Intestinal\nStromal Tumor)")
                .font(.system(size: 18))
                .multilineTextAlignment(.center)
                .padding(.top, 18)

            Spacer().frame(height: 16)

            // KIT Button
            Button(action: {
                // Add navigation or action here if required
            }) {
                Text("KIT")
                    .foregroundColor(.white)
                    .font(.system(size: 18, weight: .medium))
                    .frame(maxWidth: .infinity, minHeight: 48)
                    .background(Color(red: 0.51, green: 0.84, blue: 1.0))
                    .cornerRadius(10)
            }
            .padding(.horizontal, 24)
            .padding(.top, 4)

            // Numbered List
            VStack(spacing: 10) {
                Text("1. Diffuse strong positivity")
                    .font(.system(size: 18))
                    .multilineTextAlignment(.center)
                Text("2. Focal weak pattern")
                    .font(.system(size: 18))
                    .multilineTextAlignment(.center)
                Text("3. Dot like peri-nuclear\nstaining")
                    .font(.system(size: 18))
                    .multilineTextAlignment(.center)
                Text("4. Membranous pattern")
                    .font(.system(size: 18))
                    .multilineTextAlignment(.center)
            }
            .padding(.top, 28)
            .padding(.horizontal, 18)

            Spacer()
        }
        .background(Color.white.ignoresSafeArea())
        .navigationBarBackButtonHidden(true)
    }
}

struct GIST_KITScreen_Previews: PreviewProvider {
    static var previews: some View {
        NavigationStack {
            GIST_KITScreen()
        }
    }
}

