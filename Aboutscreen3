import SwiftUI

struct Aboutscreen3: View {
    @State private var patientID: String = ""
    @Environment(\.dismiss) private var dismiss
    @State private var goToTissuetypes = false

    var body: some View {
        VStack(spacing: 0) {
            // Top bar: Back arrow + Title on SAME LINE
            HStack {
                Button(action: { dismiss() }) {
                    Image(systemName: "arrow.left")
                        .font(.system(size: 26, weight: .bold))
                        .foregroundColor(.black)
                        .padding(.leading, 10)
                }
                Spacer()
                Text("Patient details")
                    .font(.system(size: 28, weight: .medium))
                    .foregroundColor(.black)
                Spacer()
            }
            .padding(.top, 28)
            .padding(.bottom, 10)

            Text("please enter the patients idnumber to proceed")
                .font(.system(size: 14))
                .foregroundColor(.black.opacity(0.8))
                .multilineTextAlignment(.center)
                .padding(.horizontal, 20)
                .padding(.bottom, 46)

            TextField("enter id number of patient", text: $patientID)
                .font(.system(size: 18, weight: .bold))
                .padding(.vertical, 10)
                .padding(.horizontal, 8)
                .background(Color(UIColor.lightGray).opacity(0.36))
                .cornerRadius(2)
                .frame(maxWidth: .infinity)
                .padding(.horizontal, 24)
                .foregroundColor(.black)

            Spacer().frame(height: 36)

            NavigationLink(destination: Tissuetypes(), isActive: $goToTissuetypes) {
                EmptyView()
            }
            Button(action: {
                goToTissuetypes = true
            }) {
                Text("search")
                    .font(.system(size: 20, weight: .bold))
                    .foregroundColor(.white)
                    .frame(maxWidth: .infinity)
                    .padding()
                    .background(Color(red: 0.54, green: 0.87, blue: 1.0))
                    .cornerRadius(8)
            }
            .padding(.horizontal, 24)

            Spacer()
        }
        .background(Color.white.ignoresSafeArea())
        .navigationBarBackButtonHidden(true)
        .navigationBarHidden(true)
    }
}

#Preview {
    NavigationStack {
        Aboutscreen3()
    }
}
